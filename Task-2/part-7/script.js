// Identifiy the variable
let secondNumber = prompt("Please Enter the username ...");
let firstNumber = prompt("Please Enter the Password ...");

// events
let password = "123";
let username = "ahmad";

if (firstNumber == password && secondNumber == username) {
  alert("Login action is Correct");
} else {
  alert("Login action isn't Correct");
}
